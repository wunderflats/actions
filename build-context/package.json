{
  "name": "build-context",
  "private": true,
  "main": "dist/main.js",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "5.1.6",
    "@vercel/ncc": "^0.36.1"
  },
  "scripts": {
    "build": "rm -rf ./dist && ncc build ./src -o ./dist -m"
  }
}
