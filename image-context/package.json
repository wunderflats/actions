{
  "name": "image-args-pull-requests",
  "version": "1.0.0",
  "main": "main.js",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  },
  "scripts": {
    "build": "rm -rf ./dist && ncc build ./src -o ./dist -m"
  }
}