name: "Upload results to the test dashboard"
description: "Upload specific files to the test dashboard"
author: "Wunderflats GmbH"
runs:
  using: "node20"
  main: "dist/index.js"

inputs:
  api-url:
    description: "API_URL"
    required: true
  repository:
    description: "REPOSITORY"
    required: true
  branch:
    description: "BRANCH"
    required: true
  test-suite:
    description: "TEST_SUITE"
    required: true
  test-file-type:
    description: "TEST_FILE_TYPE"
    required: true
  commit-hash:
    description: "COMMIT_HASH"
    required: true
  job-id:
    description: "JOB_ID"
    required: true
  files:
    description: "FILES"
    required: true
  dashboard-push-token:
    description: "DASHBOARD_PUSH_TOKEN"
    required: true
